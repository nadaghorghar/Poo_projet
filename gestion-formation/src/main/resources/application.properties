# Database
spring.datasource.url=jdbc:mysql://localhost:3306/gestion_formation?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT secret (change this to any long random string)
app.jwt.secret=MonSecretJWTTresLongPourGestionFormation2025SuperSecret
app.jwt.expiration=86400000

# Allow Angular (port 4200) to call the API
spring.web.cors.allowed-origins=http://localhost:4200

server.port=8080